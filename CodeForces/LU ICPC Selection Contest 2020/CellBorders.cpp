#include <bits/stdc++.h>
using namespace std;

int main()
{
	int e,d,n,aux;
	bool deu=true;
	vector<int> vet;
	cin>>n;
	for( int i=0; i<n; i++)
	{
		cin>>aux;
		vet.push_back(aux);
	}
	if(vet[0]==0)
		e=0;
	else if(vet[1]==2)
		e=1;
	else
	{
		if(n>1 && vet[1]>0)
			e=0,d=1;
		else
	}
	
	for( int i=1; i<n; i++)
	{
		
	}
}
